export default function Dashboard() {
  return (
    <div
      style={{
        minHeight: "100vh",
        background: "#000",
        color: "#fff",
        padding: "40px",
      }}
    >
      <h1>Welcome to HireEdge Dashboard</h1>
      <p>You are logged in successfully via Google.</p>
    </div>
  );
}
